# Anthropic API key for Claude models
ANTHROPIC_API_KEY=

# OpenAI API key (optional, for GPT models)
OPENAI_API_KEY=

# BuildrFlags configuration (optional — disables flag gating if not set)
BUILDFLAGS_API_URL=
BUILDFLAGS_SDK_KEY=
